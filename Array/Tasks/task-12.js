// Write a program that takes an array of numbers and creates a new array with the square of each number using the `map()` method.

let num=[1,2,3,4,5];
for(let i=0;i<num.length;i++){
    let sq=i**2;
    console.log("Square of Array:"+sq);
}
